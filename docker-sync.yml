version: 2

options:
  compose-file-path: './docker-compose.yml'
  varbose: true
syncs:
  app-app-sync:
    src: './app'
  app-bin-sync:
    src: './bin'
  app-common-sync:
    src: './common/'
  app-src-sync:
    src: './src'
  app-web-sync:
    src: './web'
